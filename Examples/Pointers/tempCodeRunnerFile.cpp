int a, *b;

   //    a = 5;
   //    b = new int;  // allocate mem for a int in memory and set it to pointer
   //    b *b = 20;      //  *b = get value where pointer points to

   //    cout << a << " " << *b << endl;

   //    ////

   //    int a = 10;
   //    int* p;
   //    int* q;

   //    p = q = &a;  // "p" y "q" van a apuntar al valor de "a"

   //    cout << *q << " " << *b << endl;
